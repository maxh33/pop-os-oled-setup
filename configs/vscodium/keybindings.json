[
    // Keyboard workaround for Wayland Ctrl+Click issue
    // Use Ctrl+Shift+O to open detected links in terminal
    {
        "key": "ctrl+shift+o",
        "command": "workbench.action.terminal.openDetectedLink",
        "when": "terminalFocus"
    }
]
